spring:
  datasource:
    url: jdbc:h2:mem:java-inheritance;
    driverClassName: org.h2.Driver
    username: admin
    password:
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 8080
